/**
Dominant Writing Direction
- input: string
# - umwandeln in array -> split in Wörter? oder in zeichen?
# - für jedes element des array überprüfen zu welcher sprache es gehört
	-> charCodeAt(0)

map 
function direction(string){
überprüfe charCodeAt(position)
alle writing directions als variablen = 0
dann +1 für writing direction variable zu der der charCode gehört
}

string.map()

reducer = (a, b) => ???
string.reduce(reducer)
 */